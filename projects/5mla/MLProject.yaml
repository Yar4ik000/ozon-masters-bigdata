name: MLProject

conda_env: conda.yaml

entry_points:
    main:
        parameters:
            train_path: path
            C: {type: float, default: 1.0}
        command: "python train.py {train_path} {C}"

